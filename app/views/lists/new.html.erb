<div class="row justify-content-center">
  <div class="col-md-6 col-lg-5">
    <div class="glass-card p-4 fade-in">
      <div class="text-center mb-4">
        <h1 class="h3 mb-2">📁 Nouvelle Liste</h1>
        <p class="text-secondary-custom">Créez une nouvelle liste de films</p>
      </div>

      <%= form_with model: @list, local: true do |f| %>
        <div class="mb-4">
          <%= f.label :name, "Nom de la liste", class: "form-label" %>
          <%= f.text_field :name,
              class: "form-control form-control-lg",
              placeholder: "Ex: Mes favoris, À voir absolument...",
              required: true,
              autofocus: true %>
        </div>

        <div class="d-grid gap-2">
          <%= f.submit "Créer la liste", class: "btn btn-primary btn-lg" %>
          <%= link_to "Annuler", lists_path, class: "btn btn-outline-light" %>
        </div>
      <% end %>
    </div>
  </div>
</div>
